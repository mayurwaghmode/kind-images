version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: mayurwaghmode/kind-runner-x86
    steps:
      - run: echo "Hi I'm on Runners!"
